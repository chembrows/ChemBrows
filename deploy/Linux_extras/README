INSTALL (Ubuntu)
================

First, extract the tar.gz package.

Then, make ChemBrows executable, if it is not already:

sudo chmod +x Chembrows

To start ChemBrows, run in a terminal:

./ChemBrows

It should run out of the box (tested on Ubuntu 16.04 and Arch Linux).

If you encounter a bug, please report it here:
https://github.com/chembrows/ChemBrows

Enjoy !
